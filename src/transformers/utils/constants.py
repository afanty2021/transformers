"""
Transformers图像处理常量定义模块

该模块定义了图像预处理中使用的标准化常量，包括不同数据集的均值和标准差。
这些常量用于图像归一化，是图像预处理管道的重要组成部分。

主要功能：
- 提供ImageNet数据集的标准化参数
- 提供OpenAI CLIP模型的标准化参数
- 支持多种图像预处理管道

使用场景：
- 图像分类任务的预处理
- 多模态模型的图像编码
- 计算机视觉任务的标准化
"""

# ImageNet数据集默认标准化参数
# 这些值基于ImageNet训练集的统计信息计算得出
# 用于将图像像素值从[0, 1]范围标准化为标准正态分布
IMAGENET_DEFAULT_MEAN = [0.485, 0.456, 0.406]  # RGB通道的均值
IMAGENET_DEFAULT_STD = [0.229, 0.224, 0.225]   # RGB通道的标准差

# ImageNet标准标准化参数（简化版本）
# 使用统一的0.5均值和标准差，将像素值从[0, 1]映射到[-1, 1]范围
# 常用于生成模型和一些特定的预训练模型
IMAGENET_STANDARD_MEAN = [0.5, 0.5, 0.5]        # RGB通道统一均值
IMAGENET_STANDARD_STD = [0.5, 0.5, 0.5]         # RGB通道统一标准差

# OpenAI CLIP模型标准化参数
# 这些值是CLIP模型在预训练时使用的标准化参数
# 基于大规模图文对数据集计算得出，确保与CLIP模型的训练分布一致
OPENAI_CLIP_MEAN = [0.48145466, 0.4578275, 0.40821073]  # CLIP RGB通道均值
OPENAI_CLIP_STD = [0.26862954, 0.26130258, 0.27577711]   # CLIP RGB通道标准差
